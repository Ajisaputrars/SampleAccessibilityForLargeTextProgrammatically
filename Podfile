# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'TryAccessibilityLargeTextConstraint' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TryAccessibilityLargeTextConstraint
  pod 'SnapKit'

  target 'TryAccessibilityLargeTextConstraintTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TryAccessibilityLargeTextConstraintUITests' do
    # Pods for testing
    inherit! :search_paths
  end

end
